# These requirements are not included in the bundled ``vendor/`` lib, and
# must be installed on target machines via system package managers.

py-bcrypt==0.4
MySQL-python==1.2.4

# only needed for getting test code coverage
coverage==3.6
